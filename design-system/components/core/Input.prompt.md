Text field on a raised Ink surface with hairline border; label above, Trace meta line below.
```jsx
<Input label="Guest name" placeholder="Ana Torres" meta="shown on the arrival card" />
```
Focus styling comes from the page; keep radius 0.
