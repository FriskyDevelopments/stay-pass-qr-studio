Credential lifecycle chip — outlined, one state color from the alternate beacons.
```jsx
<Tag state="valid">issued</Tag> <Tag state="revoked">revoked</Tag>
```
States: valid (mint), expiring (amber), revoked (coral), accent (sky), neutral (trace).
